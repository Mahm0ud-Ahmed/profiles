// colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF3F5F90); 
  static const Color secondary = Color(0xFF10B981); 

  // Status colors
  static const Color unverified = Color(0xFFFF5449);
  static const Color pending = Color(0xFF74777F);

  // Background colors
  static const Color background = Color(0xFFF3F3FA);
  static const Color cardBackground = Colors.white;
  static const Color editButtonBackground = Color(0xFFEFF2FE);
  static const Color currentlyUseBackground = Color(0xFFE4FBF2);

  // Text colors
  static const Color textPrimary = Color(0xFF43474E);
  static const Color textSecondary = Color(0xFF3F5F90);
  static const Color textSwitch = Colors.white;

  // UI Element colors
  static const Color divider = Color(0xFFE2E2E9);
  static const Color bottomNavInactive = Color(0xFFB3B9C4);
}
